from setuptools import setup, find_packages

setup(
    name='dptdr',
    version='0.0.1',
    packages=find_packages("src"),
    package_dir={'': 'src'},
    license='Apache 2.0',
    author='',
    author_email='',
    python_requires='>=3.7',
    install_requires=[ ]
)
